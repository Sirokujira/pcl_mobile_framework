// PCLMobileBasicSampleApp.swift
//
// Minimal SwiftUI host for the basic PCLMobile demo. Runs on the iOS
// Simulator -- no ARKit, no camera, no special device requirements.

import SwiftUI

@main
struct PCLMobileBasicSampleApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
