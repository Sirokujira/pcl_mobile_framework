name: MetalAROverlay
options:
  bundleIdPrefix: io.github.sirokujira
  deploymentTarget:
    iOS: "17.0"
  developmentLanguage: en
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    ENABLE_BITCODE: NO
    MTL_ENABLE_DEBUG_INFO: INCLUDE_SOURCE

targets:
  MetalAROverlay:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MetalAROverlay
    info:
      path: MetalAROverlay/Info.plist
      properties:
        NSCameraUsageDescription: "ARKit uses the camera to render an AR overlay with Metal."
        UIRequiredDeviceCapabilities:
          - arm64
          - arkit
          - iphone-lidar
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.sirokujira.MetalAROverlay
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_FILE: MetalAROverlay/Info.plist
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/Frameworks"

schemes:
  MetalAROverlay:
    build:
      targets:
        MetalAROverlay: all
    run:
      config: Debug
    archive:
      config: Release
